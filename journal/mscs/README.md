# MSCS submission candidate

This directory no longer contains the October 2025 “Fourth Foundation” draft.
That draft made claims not supported by the current scoped theorem package and
must not be submitted or circulated as the current paper.

`draft_Proof_hi.pdf` is retained only as a compatibility filename. Its bytes
must be identical to `arxiv/main.pdf`, whose current title is:

> Exact Descent of Ternary Relations along Incidence Bisimulation Quotients

The source of truth is `arxiv/main.tex`. Do not edit a second journal copy until
a journal-specific class/template is actually required. At that point, add a
reproducible transformation or an independently checked source instead of
manually allowing the two manuscripts to drift.

The mathematical manuscript is an arXiv-ready first preprint. Journal
submission remains gated on subscription-database/cited-reference coverage and
an external coalgebra/incidence specialist review. See
`docs/papers/journal-submission-gate.tsv`.
